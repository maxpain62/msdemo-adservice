apiVersion: v1
kind: Service
metadata:
  name: {{ include "msdemo-adservice-chart.fullname" . }}
  labels:
    {{- include "msdemo-adservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msdemo-adservice-chart.selectorLabels" . | nindent 4 }}
