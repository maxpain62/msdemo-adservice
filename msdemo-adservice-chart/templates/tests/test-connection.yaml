apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msdemo-adservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "msdemo-adservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msdemo-adservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
